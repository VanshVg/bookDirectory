{
  "name": "bookdirectory",
  "version": "1.0.0",
  "description": "This contains end points related to book directory which will store books if it's already not stored, show stored books and it will update and delete existing books.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Vansh Gandhi",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "mongoose": "^6.6.1",
    "nodemon": "^2.0.20"
  }
}
